# Django Configuration
DEBUG=True
SECRET_KEY=django-insecure-replace-this-with-a-real-random-secret-key-in-production
ALLOWED_HOSTS=127.0.0.1,localhost

# CORS Configuration (comma separated origins)
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Database configuration (PostgreSQL)
# Format: postgres://user:password@host:port/database_name
# If DATABASE_URL is not set, Django will fall back to local SQLite for development
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/ulinkup
